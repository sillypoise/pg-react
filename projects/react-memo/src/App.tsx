import { useState } from "react";

function App() {
    const [count, setCount] = useState(0);

    return (
        <main>
            <article>
                <h1>React memo</h1>
            </article>
        </main>
    );
}

export default App;
